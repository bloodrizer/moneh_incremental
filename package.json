{
  "name": "moneh",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.tsx --bundle --minify --sourcemap  --target=es2020 --outfile=main.js",
    "watch": "esbuild index.tsx --bundle --minify --sourcemap  --target=es2020 --outfile=main.js --servedir=. --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.19.4"
  },
  "dependencies": {
    "@types/react": "^18.2.23",
    "@types/react-dom": "^18.2.8",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "^5.2.2"
  }
}
